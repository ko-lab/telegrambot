# Setup 
Create a file called `bot_config.py`. Into that file, write:

```API_KEY = "[your telegram api key]"```

For the api key of the original telegram bot check the `bot_config.py` on the Orange Pi or ask Tobias. 